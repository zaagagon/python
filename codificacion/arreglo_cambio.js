// Declaramos un arreglo de colores
let colores = ['rojo', 'verde', 'azul'];

// Modificamos el segundo elemento (índice 1)
colores[1] = 'amarillo';

// Acceder al segundo elemento modificado
const nuevoColor = colores[1];
console.log('Nuevo color:', nuevoColor); // Resultado: 'amarillo'
